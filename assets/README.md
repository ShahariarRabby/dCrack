This folder is intended to store assets used by the main `README.md`.

Please place an image file named `dcrack-email.png` here, containing the email address for dataset access rendered as text in the image. The root `README.md` references this image to display the contact email without exposing it as plain text.

